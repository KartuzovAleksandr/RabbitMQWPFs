<Window x:Class="OrderProducerWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OrderProducerWPF"
        mc:Ignorable="d"
        Title="RabbitMQWPF" Height="450" Width="800" WindowStartupLocation="CenterScreen" Closing="Window_Closing">
    <Grid Margin="10">
        <StackPanel>
            <Button Content="Заполнить случайными данными" Click="Random_Click" Width="200" HorizontalAlignment="Center" Margin="0,10,0,10"/>
            <TextBox x:Name="txtCustomer" Text="Покупатель (Customer)" Margin="0,10,0,10"/>
            <TextBox x:Name="txtProduct" Text="Товар (Product)" Margin="0,0,0,10"/>
            <TextBox x:Name="txtQuantity" Text="Количество (Quantity)" Margin="0,0,0,20"/>
            <Button Content="Отправить заказ" Click="Send_Click" Width="120" HorizontalAlignment="Center"/>
            <DataGrid Name="DataGridOrders" Margin="20" Height="200" 
                ItemsSource="{Binding Orders}"
                AutoGenerateColumns="False" 
                IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Покупатель" 
                           Binding="{Binding CustomerName}" Width="*"/>
                    <DataGridTextColumn Header="Товар" 
                           Binding="{Binding ProductName}" Width="*"/>
                    <DataGridTextColumn Header="Кол-во" 
                           Binding="{Binding Quantity}" Width="*"/>
                    <DataGridTextColumn Header="Цена" 
                           Binding="{Binding Price, StringFormat={}{0:N2} ₽}" Width="*"/>
                    <DataGridTextColumn Header="Сумма" 
                           Binding="{Binding TotalPrice, StringFormat={}{0:N2} ₽}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</Window>
